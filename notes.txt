https://sheppark-ddn-shep.c9.io/

async
express --> unintalled
socket.io

npm install -g 
    nodemon
    bower
    grunt-cli

npm install --save
    express
    jade

npm install --save-dev
    grunt
    // grunt-autoprefixer // npm update caniuse-db = Update the vendor prefix database
    grunt-contrib-sass
    grunt-contrib-watch
    grunt-postcss pixrem autoprefixer cssnano
    grunt-riot riot
    
    // TODO: protractor karma

bower install
